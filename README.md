<div align="center">

<img src="build/icon.png" width="112" alt="Alcode Editor" />

# Alcode Editor

**استوديو المستندات من شركة Alcode** — تطبيق مكتبي لتحرير وتحويل وتنظيم **كل صيغ المستندات**:
PDF و Word و RTF و ODT و Excel و CSV و PowerPoint و EPUB و Markdown و HTML والنصوص والشيفرة والصور.
يعمل بالكامل على جهازك بدون إنترنت وبدون رفع أي ملف إلى أي خادم.

*A desktop studio for editing, converting, compressing and organising every document
format — PDF, Word, Excel, PowerPoint, EPUB and more. Fully offline.*

</div>

---

## ⬇️ التحميل المباشر — Direct download

> الروابط تعمل بعد اكتمال أول إصدار على صفحة [Releases](../../releases). إن لم تفتح بعد، فالبناء ما زال جارياً.

| النظام / Platform | نوع الملف | رابط التحميل |
| --- | --- | --- |
| 🪟 **Windows 10/11 (64-bit)** | مثبّت `.exe` | [تحميل / Download](../../releases/latest/download/Alcode-Editor-1.1.0-x64-Setup.exe) |
| 🪟 **Windows (نسخة محمولة)** | `Portable.exe` بدون تثبيت | [تحميل / Download](../../releases/latest/download/Alcode-Editor-1.1.0-Portable.exe) |
| 🍎 **macOS — Apple Silicon (M1/M2/M3/M4)** | `.dmg` | [تحميل / Download](../../releases/latest/download/Alcode-Editor-1.1.0-arm64.dmg) |
| 🍎 **macOS — Intel** | `.dmg` | [تحميل / Download](../../releases/latest/download/Alcode-Editor-1.1.0-x64.dmg) |

📦 **كل الإصدارات:** [صفحة الإصدارات / All releases](../../releases/latest)

### ملاحظات التثبيت

النسخ الحالية **غير موقّعة رقمياً**، لذلك:

- **ويندوز:** قد تظهر شاشة SmartScreen زرقاء → اضغط **More info** ثم **Run anyway**.
- **macOS:** بعد نقل التطبيق إلى مجلد Applications، افتحه أول مرة بالضغط عليه بزر الفأرة الأيمن ثم **Open**
  (أو من *System Settings → Privacy & Security → Open Anyway*).

---

## ✨ المميزات

### العرض والتصفح
- عارض PDF سريع مبني على pdf.js مع تمرير متصل أو صفحة‑بصفحة.
- تكبير وتصغير، ملء العرض، ملء الصفحة، حجم فعلي، وتدوير العرض.
- شريط مصغّرات جانبي بتحميل كسول (يفتح مستندات بمئات الصفحات دون تأخير).
- فهرس المستند (Outline) والانتقال المباشر لأي قسم.
- بحث نصي كامل داخل المستند مع مقتطفات ونتائج قابلة للنقر.

### تنظيم الصفحات
- إعادة الترتيب بالسحب والإفلات، وعكس الترتيب.
- تدوير، حذف، تكرار، واستخراج الصفحات المحددة إلى ملف جديد.
- إدراج صفحة فارغة أو إدراج صفحات من ملف PDF آخر في أي موضع.
- تحديد متعدد مع شريط أدوات يعمل على التحديد مباشرة.

### التحرير والتعليقات
- نص حر، تظليل، رسم يدوي، خطوط، مستطيلات، بيضاوي، صور، وتنقيح (Redact).
- لوح توقيع بالفأرة/اللمس، ووضع التوقيع في أي مكان على الصفحة.
- تحكم كامل في اللون، السماكة، الشفافية، حجم الخط، والتعبئة.
- تحريك وحذف أي عنصر، ثم **دمج التعليقات نهائياً في الملف**.

### صندوق الأدوات (25 أداة)
| الصفحات | التحسين والحماية | المحتوى |
| --- | --- | --- |
| دمج ملفات | ضغط الملف | علامة مائية (نص أو شعار) |
| تقسيم (نطاق/عدد/كل صفحة) | حماية بكلمة مرور AES‑256 | ترقيم الصفحات (أرقام عربية أو لاتينية) |
| تقسيم حسب الحجم | فك الحماية | ترويسة وتذييل |
| استخراج صفحات | تحسين وإصلاح | خلفية الصفحة |
| حذف صفحات | بيانات المستند (Metadata) | ختم جاهز (مسودة/سري/معتمد) |
| تدوير الصفحات | | استخراج الصور المضمّنة |
| دمج صفحات في ورقة (2/4/6/9) | | النماذج: عرض وتعبئة وتسطيح |
| تغيير قياس الصفحة | | مقارنة مستندين سطراً بسطر |
| قص الهوامش | تصغير حجم أي ملف | الفهرس والمرفقات |

### محرر المستندات الشامل

محرر واحد يفتح **أكثر من ٤٠ امتداداً** ويختار الواجهة المناسبة لكل نوع:

| النوع | الصيغ | ما يمكنك فعله |
| --- | --- | --- |
| **مستندات نصية** | `docx` `doc` `rtf` `odt` `epub` `pptx` `md` `html` `txt` | تحرير غني كامل |
| **جداول بيانات** | `xlsx` `xlsm` `xls` `ods` `csv` `tsv` | شبكة قابلة للتحرير بأوراق متعددة |
| **نصوص وشيفرة** | `json` `xml` `yml` `sql` `js` `ts` `py` `css` … | محرر بترقيم أسطر وتنسيق تلقائي |
| **صور** | `png` `jpg` `webp` `gif` `bmp` `tiff` | عرض وضغط وتحويل |

**التحرير النصي:** عريض/مائل/تسطير/يتوسطه خط · ألوان النص والتمييز · الخط والحجم ·
عناوين ومحاذاة وقوائم واقتباسات · إدراج جداول (مع إضافة/حذف صفوف وأعمدة أثناء الكتابة)
وصور وروابط وفواصل صفحات · لصق آمن مُعقَّم · تدقيق إملائي · تكبير 50%–250% ·
لوحة مخطط للتنقل بين العناوين · بحث واستبدال بتعبير نمطي ومطابقة حالة الأحرف وكلمة كاملة ·
اتجاه RTL/LTR · عدّاد كلمات وحروف وفقرات وزمن قراءة · ستة قوالب جاهزة
(خطاب، تقرير، فاتورة، سيرة ذاتية، محضر اجتماع، مستند فارغ).

**الجداول:** إضافة/حذف صفوف وأعمدة · ترتيب تصاعدي وتنازلي · تحديد بالسحب مع مجموع
ومتوسط فوري · أوراق متعددة بإعادة تسمية · تنقّل بالأسهم و Tab و Enter.

**التصدير:** DOCX · PDF · RTF · ODT · HTML · Markdown · TXT · XLSX · ODS · CSV · TSV.

### قراءة صحيحة للعربية والإنجليزية

الملفات النصية لا تحمل تصريحاً بترميزها، فيكتشفه التطبيق بنفسه:

- علامة الترتيب (BOM) لـ UTF‑8 و UTF‑16 بنوعيها.
- تحقّق صارم من صحة UTF‑8.
- عند الفشل يجرّب **Windows‑1256** و **ISO‑8859‑6** و Windows‑1252 و Windows‑1251
  ويحسب نقاطاً لكل ترميز بحسب كمية الحروف الحقيقية التي ينتجها — فيختار الصحيح.
- تطبيع أشكال الحروف العربية المتصلة (Presentation Forms) القادمة من ملفات PDF و DOC القديمة.
- كشف تلقائي لاتجاه النص من موازنة الحروف القوية.

النتيجة: ملف CSV عربي قديم بترميز Windows‑1256 يُفتح بنص سليم بدل الرموز المشوّهة.

### تصغير حجم الملفات
- **PDF:** سباق بين إعادة بناء بلا فقد وإعادة رسم الصفحات كصور — ويُبقى الأصغر فقط.
- **الصور:** إعادة ترميز عبر canvas مع حدّ لأقصى بُعد، وتجربة JPEG و WebP واختيار الأصغر.
- **ملفات Office (DOCX/XLSX/PPTX/ODT/ODS/EPUB):** فكّ الحزمة، وضغط الصور المضمّنة بداخلها،
  ثم إعادة الحزم بأقصى مستوى deflate.
- في كل الحالات: إن لم ينتج الضغط ملفاً أصغر، يُبقى الأصل كما هو ويُخبرك التطبيق بذلك.

### التحويل بين الصيغ
`PDF → صور (PNG/JPG بأي دقة)` · `صور → PDF` · `PDF → نص` · `PDF → Word`
`Word → PDF` · `Word → HTML` · `نص/Markdown → PDF` · `HTML → PDF`

بالإضافة إلى التصدير من المحرر إلى أي صيغة مدعومة للكتابة.

### التصميم والتجربة
- واجهة مستوحاة من لغة تصميم Apple: أسطح شفافة، حدود شعرية، زوايا ناعمة، وحركات نابضة.
- وضع فاتح وداكن وتلقائي يتبع النظام، مع ستة ألوان تمييز.
- عربي كامل بواجهة RTL، وإنجليزي بواجهة LTR — قابل للتبديل فوراً.
- لوحة أوامر (`Ctrl/Cmd + K`) للوصول لأي أداة أو شاشة.
- تراجع/إعادة على مستوى المستند، وسجل ملفات حديثة.
- شريط عنوان مخصص يتكيّف مع ويندوز وماك.

### الخصوصية
كل المعالجة تتم محلياً داخل التطبيق. **لا يوجد أي اتصال شبكي**، وسياسة أمان المحتوى (CSP)
في التطبيق تمنع أي طلب خارجي. ملفاتك لا تغادر جهازك أبداً.

---

## 🛠 التقنيات

| الطبقة | التقنية |
| --- | --- |
| الغلاف المكتبي | Electron 33 (frameless, contextIsolation, sandboxed renderer) |
| الواجهة | React 19 · TypeScript · Framer Motion · Zustand · Lucide |
| عرض PDF | pdfjs-dist (worker مُضمّن محلياً) |
| تحرير PDF | @cantoo/pdf-lib (يشمل تشفير AES‑256) |
| Word / RTF / ODT | mammoth · docx · محلّل RTF وODT مكتوب داخل المشروع |
| الجداول | SheetJS (xlsx, xls, ods, csv) |
| PPTX / EPUB | JSZip + DOMParser (استخراج مباشر من الحزمة) |
| تنقية HTML | DOMPurify — كل مستند وارد يمرّ عبرها قبل عرضه |
| Markdown | محوّل داخلي + Turndown للتصدير |
| توليد PDF من HTML | `webContents.printToPDF` في نافذة Chromium خفية |
| البناء والتغليف | electron-vite · electron-builder (NSIS + DMG) |

**دعم العربية في PDF:** خطوط PDF القياسية لا تدعم الحروف العربية. يكتشف التطبيق النصوص
غير اللاتينية ويرسمها عبر محرك النصوص في Chromium بدقة 4× ثم يضمّنها كصورة PNG شفافة —
وهذا يعطي تشكيلاً واتصالاً صحيحاً للحروف دون شحن أي مكتبة تشكيل.

---

## 💻 التشغيل من المصدر

```bash
git clone https://github.com/almarar88/liwapdf.git
cd liwapdf
npm install

npm run dev            # تشغيل في وضع التطوير
npm run typecheck      # فحص الأنواع
npm run build          # بناء الحزم
npm run dist:win       # مثبّت ويندوز (يتطلب ويندوز أو wine)
npm run dist:mac       # حزمة macOS (تتطلب macOS)
```

### إصدار نسخة جديدة

الملفات التنفيذية تُبنى تلقائياً على GitHub Actions لويندوز وماك:

```bash
git tag v1.1.0
git push origin v1.1.0
```

سير العمل في `.github/workflows/release.yml` يبني النسختين وينشرهما في Release واحد
مع الروابط المباشرة أعلاه. يمكن تشغيله يدوياً أيضاً من تبويب **Actions → Build & Release → Run workflow**.

---

## ⌨️ اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
| --- | --- |
| `Ctrl/Cmd + O` | فتح ملف |
| `Ctrl/Cmd + S` | حفظ |
| `Ctrl/Cmd + K` | لوحة الأوامر |
| `Ctrl/Cmd + Z` | تراجع |
| `Ctrl/Cmd + Shift + Z` | إعادة |
| `Ctrl/Cmd + H` | بحث واستبدال في المحرر |
| `Tab` / `Enter` / الأسهم | التنقل بين خلايا الجدول |

---

## 📁 بنية المشروع

```
src/
├── main/          عملية Electron الرئيسية: النوافذ، الحوارات، نظام الملفات، طباعة PDF
├── preload/       جسر contextBridge الآمن بين الواجهة والنظام
├── shared/        الأنواع المشتركة بين العمليتين
└── renderer/src/
    ├── lib/pdf/        محرك عمليات PDF (تنظيم، علامات، حماية، ضغط، تعليقات)
    ├── lib/docx/       قراءة وكتابة Word وتوليد HTML للطباعة
    ├── lib/documents/  طبقة الصيغ: كشف، قراءة، كتابة (RTF, ODT, PPTX, EPUB, جداول)
    ├── lib/text/       كشف الترميز واتجاه النص
    ├── lib/compress/   الضغط الموحّد لكل الصيغ
    ├── components/     مكوّنات الواجهة ونظام التصميم
    ├── views/          الشاشات وصندوق الأدوات ومحررات النص والجداول والشيفرة
    ├── store/          حالة التطبيق (Zustand)
    └── i18n/           القاموس العربي/الإنجليزي
```

---

<div align="center">

صُنع بعناية في **Alcode** — © 2026

</div>
